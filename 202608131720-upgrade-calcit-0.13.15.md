# Upgrade respo/value.calcit to Calcit 0.13.15

## Change summary

- Synced `deps.cirru`, `@calcit/procs`, and the Yarn lockfile to Calcit 0.13.15.
- Removed unused deprecated `calcit-lang/lilac` and `calcit-lang/memof` dependencies and their snapshot module entries.
- Kept the existing single `calcit.cirru` snapshot and canonical typed runtime behavior unchanged.

## Knowledge point

When a project does not reference a deprecated module, remove both its dependency declaration and entry-level module path together. Re-run dependency linking and the actual JS/Vite build to prove the removal did not only pass static preprocessing.
