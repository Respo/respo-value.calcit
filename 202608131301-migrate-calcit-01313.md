# Migrate respo-value to Calcit 0.13.13

- Replace the unreadable legacy EDN `calcit.cirru` and redundant `compact.cirru` with one canonical modern snapshot.
- Rename deprecated `package.cirru` to `deps.cirru`; update Calcit, respo, lilac, memof, and JavaScript runtime dependencies.
- Make value-node state access explicit with `get` and `option:unwrap-or`, including concrete empty Map expressions for JS codegen.
- Update CI from downloaded 0.8.0-alpha binaries to `calcit-lang/setup-cr`, Corepack Yarn, check-only, JS generation, and Vite build.
- Verify with Calcit 0.13.13 check-only, JS output, and Vite production build.
