# Align respo-value with Calcit 0.13.75

- Upgrade Calcit and `@calcit/procs` to 0.13.75 and move to the current typed Respo boundary.
- Convert map entry pairs from `Set` to `List` before `map-indexed`, and migrate deprecated `tag-match` to `match`.
- Pin the exact reviewed Respo #128 commit until its typed DOM APIs are released as SemVer.
- Add a native Cirru EDN quality baseline and mark it generated in `.gitattributes`.
- Split CI into dependency, static validation, and JS build phases; prevent deploy steps from running on pull requests.
- The project currently has no definition-attached tests, so validation relies on check-only, static gates, JS codegen, and the Vite production build. Tracked by issue #18.
